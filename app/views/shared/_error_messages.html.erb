<% if obj.errors.any?  %>
  <div class="info">
    <div class="alert alert-danger">
      <span>The form contain: <%= pluralize(obj.errors.count, "error") %>.</span>
      <ul>
        <% obj.errors.each do |error| %>
         <li><%=  error.full_message %></li>
        <% end %>
      </ul>
    </div>
  </div>
<%  end %>